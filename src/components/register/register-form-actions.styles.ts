import { StyleSheet } from 'react-native';

import { Space } from '@/constants/theme';

export const registerFormActionsStyles = StyleSheet.create({
  submit: {
    marginTop: Space.extraSmall,
  },
});
